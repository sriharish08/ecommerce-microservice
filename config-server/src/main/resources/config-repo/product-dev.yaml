server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/product_dev
    username: root
    password: root

  flyway:
    enabled: true
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: validate

    properties:
      hibernate:
        format_sql: true

app:
  message: Product Service - DEV Environment